---
title: Introduce Parameter Object
description: เมื่อ argument กลุ่มหนึ่งเดินทางไปไหนมาไหนด้วยกันเสมอ ให้รวบเป็น object เดียว ความสัมพันธ์จะชัดขึ้นและจุดที่เรียกก็สั้นลง
sidebar:
  order: 4
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

export const paramObjectDiagram = `flowchart LR
  subgraph Before["Before"]
    A["bookFlight(<br/>from, to,<br/>startDate, endDate)"]
  end
  subgraph After["After"]
    B["bookFlight(trip)"]
    B --> C["Trip<br/>{ from, to,<br/>startDate, endDate }"]
  end
  Before -.->|"Introduce Parameter Object"| After`;

## จุดประสงค์

มองหากลุ่ม argument ที่โผล่มาด้วยกันซ้ำ ๆ ในหลาย function แล้วแทนด้วย object ตัวเดียวที่เก็บค่าทั้งชุดไว้ กลุ่มนั้นจะได้ชื่อ ความสัมพันธ์ระหว่างค่าจะชัดขึ้น และ function ที่เคยรับ argument กระจัดกระจาย ก็เหลือรับ parameter เรียบร้อยตัวเดียว

## Code Smell

ท่านี้รักษา **Long Parameter List** กับญาติคือ **Data Clump** ซึ่งก็คือค่าสองสามตัวเดิม ๆ ที่ถูกส่งเรียงติดกันตามลำดับเดิม ไล่ไปทีละ function เช่น `start` กับ `end` ที่จริง ๆ แล้วคือ *ช่วงวันที่* หรือ `latitude` กับ `longitude` ที่จริง ๆ แล้วคือ *ตำแหน่ง* พอค่าหลายตัวเดินทางกันเป็นฝูง ฝูงนั้นคือแนวคิดที่ code ยังไม่ได้ตั้งชื่อ ที่แย่กว่านั้นคือรายการยาว ๆ ที่ไม่มีชื่อกำกับ ยังเชิญชวนให้เกิดความผิดพลาดคลาสสิกอย่างการส่ง argument สลับลำดับ

## ก่อน → หลัง

การจองเที่ยวบินที่ลาก argument สี่ตัวซึ่งเกี่ยวข้องกันหลวม ๆ ไปไหนมาไหน หลัง refactor ทั้งหมดยุบเหลือ object `Trip` ตัวเดียว

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```ts
    // Before
    function bookFlight(
      from: string,
      to: string,
      startDate: string,
      endDate: string,
    ): string {
      return `Flight ${from}->${to} from ${startDate} to ${endDate}`;
    }

    const summary = bookFlight("BKK", "NRT", "2026-07-01", "2026-07-09");

    // After
    interface Trip {
      from: string;
      to: string;
      startDate: string;
      endDate: string;
    }

    function bookFlight(trip: Trip): string {
      return `Flight ${trip.from}->${trip.to} from ${trip.startDate} to ${trip.endDate}`;
    }

    const summary = bookFlight({
      from: "BKK",
      to: "NRT",
      startDate: "2026-07-01",
      endDate: "2026-07-09",
    });
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    # Before
    def book_flight(origin, destination, start_date, end_date):
        return f"Flight {origin}->{destination} from {start_date} to {end_date}"

    summary = book_flight("BKK", "NRT", "2026-07-01", "2026-07-09")

    # After
    from dataclasses import dataclass

    @dataclass
    class Trip:
        origin: str
        destination: str
        start_date: str
        end_date: str

    def book_flight(trip):
        return (
            f"Flight {trip.origin}->{trip.destination} "
            f"from {trip.start_date} to {trip.end_date}"
        )

    summary = book_flight(Trip("BKK", "NRT", "2026-07-01", "2026-07-09"))
    ```
  </TabItem>
  <TabItem label="Go">
    ```go
    // Before
    func BookFlight(from, to, startDate, endDate string) string {
        return fmt.Sprintf("Flight %s->%s from %s to %s", from, to, startDate, endDate)
    }

    summary := BookFlight("BKK", "NRT", "2026-07-01", "2026-07-09")

    // After
    type Trip struct {
        From      string
        To        string
        StartDate string
        EndDate   string
    }

    func BookFlight(t Trip) string {
        return fmt.Sprintf("Flight %s->%s from %s to %s", t.From, t.To, t.StartDate, t.EndDate)
    }

    summary := BookFlight(Trip{
        From:      "BKK",
        To:        "NRT",
        StartDate: "2026-07-01",
        EndDate:   "2026-07-09",
    })
    ```
  </TabItem>
  <TabItem label="Rust">
    ```rust
    // Before
    fn book_flight(from: &str, to: &str, start_date: &str, end_date: &str) -> String {
        format!("Flight {from}->{to} from {start_date} to {end_date}")
    }

    let summary = book_flight("BKK", "NRT", "2026-07-01", "2026-07-09");

    // After
    struct Trip {
        from: String,
        to: String,
        start_date: String,
        end_date: String,
    }

    fn book_flight(trip: &Trip) -> String {
        format!(
            "Flight {}->{} from {} to {}",
            trip.from, trip.to, trip.start_date, trip.end_date
        )
    }

    let trip = Trip {
        from: "BKK".to_string(),
        to: "NRT".to_string(),
        start_date: "2026-07-01".to_string(),
        end_date: "2026-07-09".to_string(),
    };
    let summary = book_flight(&trip);
    ```
  </TabItem>
</Tabs>

<Mermaid code={paramObjectDiagram} title="argument กระจัดกระจายสี่ตัวกลายเป็น object Trip ที่มีชื่อเพียงตัวเดียว" />

## กลไกการทำงาน

1. ระบุกลุ่มให้ได้ก่อน คือ argument ที่โผล่มาด้วยกันเสมอ แล้วตั้งชื่อให้แนวคิดที่กลุ่มนั้นแทน
2. สร้างโครงสร้างใหม่ (class, struct, record หรือ interface) ที่มี field สำหรับสมาชิกแต่ละตัวของกลุ่ม
3. เพิ่ม object ใหม่เป็น parameter ให้ function เป้าหมายตัวหนึ่ง โดยตอนแรกวางไว้ข้าง ๆ argument เดิม **รัน test**
4. ภายในเนื้อใน ให้เริ่มอ่านจาก field ของ object แทน parameter ที่กระจัดกระจาย ทีละ field โดย test ไปเรื่อย ๆ ขณะทำ
5. เมื่อเนื้อในอ่านจาก object เพียงอย่างเดียวแล้ว ให้ลบ parameter ที่กระจัดกระจายซึ่งไม่ได้ใช้แล้วออกจากลายเซ็น จากนั้นอัปเดต caller ของ function นั้นให้สร้างและส่ง object
6. ทำซ้ำกับ function อื่น ๆ ที่ใช้กลุ่มเดียวกัน เพื่อให้ทั้งหมดรับ object
7. **รัน test** หลังแก้แต่ละ function และคอยสังเกต behavior ใหม่ที่ผุดขึ้นมา เพราะพอข้อมูลมารวมกันแล้ว logic ที่ทำงานกับข้อมูลชุดนั้นมักอยากย้ายเข้าไปอยู่บน object ตามไปด้วย

## ใช้เมื่อไหร่ / ข้อแลกเปลี่ยน

แนะนำให้ใช้ parameter object เมื่อค่าชุดเดิมถูกส่งด้วยกันซ้ำ ๆ เมื่อรายการ parameter ยาวจน caller ส่งลำดับผิด หรือเมื่อคุณรู้สึกได้ว่ามีแนวคิดที่ยังไม่ได้ตั้งชื่อซ่อนอยู่ในรายการ argument object บันทึกความสัมพันธ์ไว้และให้ที่อยู่ตามธรรมชาติแก่ behavior ที่เป็นของกลุ่มนั้น

ข้อแลกเปลี่ยนคือมี type เพิ่มมาให้นิยาม และต้องเสียพิธีเล็กน้อยตอนสร้าง object ที่จุดเรียกแต่ละจุด ถ้าเป็นการเรียกสอง argument ครั้งเดียวที่ไม่เคยเกิดซ้ำ พิธีนั้นก็อาจไม่คุ้ม ผลตอบแทนจะโตขึ้นตามจำนวนการใช้ซ้ำ ยิ่งมี function ใช้กลุ่มนี้ร่วมกันมากเท่าไร object ก็ยิ่งคุ้ม และยิ่งกลายเป็นแม่เหล็กดูด method ที่เกี่ยวข้องเข้ามา

## เนื้อหาที่เกี่ยวข้อง

- [Remove Flag Argument](/refactoring/th/simplifying-apis/remove-flag-argument/)
- [Parameterize Function](/refactoring/th/simplifying-apis/parameterize-function/)
- [Rename Function/Variable](/refactoring/th/simplifying-apis/rename-function/)

<Callout title="กลุ่มของข้อมูลคือแนวคิดที่รอชื่อ">
เมื่อค่าสองตัวถูกส่งไปด้วยกันตลอด นั่นไม่ใช่ของสองชิ้น แต่คือของชิ้นเดียวที่ code ยังไม่ได้ตั้งชื่อ การตั้งชื่อให้ (เป็น `Range`, `Location` หรือ `Trip`) ได้มากกว่าแค่ทำให้จุดเรียกสั้นลง เพราะเป็นการให้ที่อยู่กับแนวคิดนั้น เป็นที่ที่ validate และ helper ของตัวเองจะย้ายมาอยู่ในภายหลัง
</Callout>

export const quizParamObject = [
  {
    q: 'Introduce Parameter Object จัดการกับกลิ่นใดเป็นหลัก?',
    options: [
      'loop ที่ช้าและคิวรีที่ซ้ำซ้อน',
      'Long Parameter List และ Data Clump',
      'การไม่มี test',
      'สถานะ global ที่เปลี่ยนแปลงได้',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมกลุ่มของ argument ที่เดินทางไปด้วยกันเสมอจึงมีนัยสำคัญ?',
    options: [
      'บ่งชี้ว่ามีบั๊กด้าน performance เสมอ',
      'มักแทนแนวคิดที่ยังไม่ได้ตั้งชื่อ ซึ่งอยากกลายเป็น object',
      'แปลว่า function สั้นเกินไป',
      'แปลว่า argument พวกนั้นไม่ได้ถูกใช้',
    ],
    answer: 1,
  },
  {
    q: 'อะไรมักเกิดขึ้นหลังจากข้อมูลถูกจัดเข้าเป็น object เดียว?',
    options: [
      'object กลายเป็นที่อยู่ตามธรรมชาติของ logic ที่ทำงานกับข้อมูลนั้น',
      'โปรแกรมทำงานช้าลง',
      'ต้องลบ test ทิ้ง',
      'ต้องส่ง argument ในลำดับย้อนกลับ',
    ],
    answer: 0,
  },
  {
    q: 'เมื่อใดที่การนำ parameter object เข้ามาอาจไม่คุ้มค่า?',
    options: [
      'เมื่อ function จำนวนมากใช้กลุ่มเดียวกันร่วมกัน',
      'สำหรับการเรียกใช้ครั้งเดียวที่ argument ไม่เคยเกิดซ้ำที่อื่น',
      'เมื่อลำดับ argument สับสนได้ง่าย',
      'เมื่อค่าต่าง ๆ แทนแนวคิดที่ชัดเจน',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="simplifying-apis/introduce-parameter-object" questions={quizParamObject} />

<ProgressTracker client:visible id="simplifying-apis/introduce-parameter-object" />
