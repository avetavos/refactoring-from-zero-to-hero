---
title: Replace Constructor with Factory Function
description: เมื่อ constructor แข็งเกินไป — เลือก subtype ไม่ได้ ตั้งชื่อสื่อความหมายก็ไม่ได้ — ให้ห่อด้วย factory function ที่ตัดสินใจแทนว่าจะสร้างอะไร
sidebar:
  order: 6
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## จุดประสงค์

เปลี่ยนจากการเรียก constructor ตรง ๆ มาเรียก factory function แทน ตัว constructor มีข้อจำกัดสองข้อ คือต้องใช้ชื่อเดียวกับ type และต้องคืน instance ของ type นั้นเป๊ะ ๆ เสมอ ส่วน factory function เป็นอิสระจากทั้งสองข้อ คือตั้งชื่อให้สื่อความหมายได้ เลือกได้ว่าจะคืน subtype ตัวไหน คืน instance ที่ cache ไว้ก็ได้ หรือจะ validate ก่อนสร้างก็ได้ caller แค่บอก factory ว่าต้องการอะไร แล้ว factory เป็นคนตัดสินใจว่าจะสร้างอย่างไร

## Code Smell

constructor ดิบ ๆ ทำให้การตัดสินใจเรื่องการสร้างรั่วไปถึง caller ทุกตัว ลองดู `new Employee("Sara", "manager")` แล้ว caller ต้องจำเองว่า string ตัวไหนแปลว่าอะไร ที่หนักกว่านั้นคือเมื่อ type มีหลายแบบ เช่น manager เป็นพนักงานคนละชนิดกับ engineer constructor เปล่า ๆ คืน subtype ที่ถูกต้องไม่ได้ caller จึงต้องแตก branch ตาม type code เอง แถมชื่อ constructor ยังถูกตรึงไว้กับ class จะเรียกว่า `hire`, `fromJson` หรือ `default` ก็ไม่ได้ ทุกข้อชี้ตรงกันว่า constructor แคบเกินไปสำหรับงานนี้

## ก่อน → หลัง

type พนักงานที่ constructor รับ stringtype หลังจากนั้น function factory เลือก subtype ที่ถูกต้องและอ่านได้ชัดเจน

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```ts
    // Before
    class Employee {
      constructor(public name: string, public type: string) {}
      monthlyBonus(): number {
        return this.type === "manager" ? 1000 : 200;
      }
    }

    const e = new Employee("Sara", "manager");

    // After
    abstract class Employee {
      constructor(public name: string) {}
      abstract monthlyBonus(): number;
    }

    class Manager extends Employee {
      monthlyBonus(): number {
        return 1000;
      }
    }

    class Engineer extends Employee {
      monthlyBonus(): number {
        return 200;
      }
    }

    function hire(name: string, role: string): Employee {
      switch (role) {
        case "manager":
          return new Manager(name);
        case "engineer":
          return new Engineer(name);
        default:
          throw new Error(`Unknown role: ${role}`);
      }
    }

    const e = hire("Sara", "manager");
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    # Before
    class Employee:
        def __init__(self, name, type):
            self.name = name
            self.type = type

        def monthly_bonus(self):
            return 1000 if self.type == "manager" else 200

    e = Employee("Sara", "manager")

    # After
    from abc import ABC, abstractmethod

    class Employee(ABC):
        def __init__(self, name):
            self.name = name

        @abstractmethod
        def monthly_bonus(self):
            ...

    class Manager(Employee):
        def monthly_bonus(self):
            return 1000

    class Engineer(Employee):
        def monthly_bonus(self):
            return 200

    def hire(name, role):
        builders = {"manager": Manager, "engineer": Engineer}
        if role not in builders:
            raise ValueError(f"Unknown role: {role}")
        return builders[role](name)

    e = hire("Sara", "manager")
    ```
  </TabItem>
  <TabItem label="Go">
    ```go
    // Before
    type Employee struct {
        Name string
        Type string
    }

    func (e Employee) MonthlyBonus() float64 {
        if e.Type == "manager" {
            return 1000
        }
        return 200
    }

    e := Employee{Name: "Sara", Type: "manager"}

    // After
    type Employee interface {
        MonthlyBonus() float64
    }

    type Manager struct{ Name string }

    func (m Manager) MonthlyBonus() float64 { return 1000 }

    type Engineer struct{ Name string }

    func (e Engineer) MonthlyBonus() float64 { return 200 }

    func Hire(name, role string) (Employee, error) {
        switch role {
        case "manager":
            return Manager{Name: name}, nil
        case "engineer":
            return Engineer{Name: name}, nil
        default:
            return nil, fmt.Errorf("unknown role: %s", role)
        }
    }

    e, err := Hire("Sara", "manager")
    ```
  </TabItem>
  <TabItem label="Rust">
    ```rust
    // Before
    struct Employee {
        name: String,
        kind: String,
    }

    impl Employee {
        fn monthly_bonus(&self) -> u32 {
            if self.kind == "manager" { 1000 } else { 200 }
        }
    }

    let e = Employee { name: "Sara".to_string(), kind: "manager".to_string() };

    // After
    trait Employee {
        fn monthly_bonus(&self) -> u32;
    }

    struct Manager {
        name: String,
    }

    impl Employee for Manager {
        fn monthly_bonus(&self) -> u32 {
            1000
        }
    }

    struct Engineer {
        name: String,
    }

    impl Employee for Engineer {
        fn monthly_bonus(&self) -> u32 {
            200
        }
    }

    fn hire(name: &str, role: &str) -> Result<Box<dyn Employee>, String> {
        match role {
            "manager" => Ok(Box::new(Manager { name: name.to_string() })),
            "engineer" => Ok(Box::new(Engineer { name: name.to_string() })),
            other => Err(format!("unknown role: {other}")),
        }
    }

    let e = hire("Sara", "manager")?;
    ```
  </TabItem>
</Tabs>

## กลไกการทำงาน

1. เขียน factory function ที่ข้างในไม่ทำอะไรนอกจากเรียก constructor เดิมแล้วคืนผลลัพธ์ ตั้งชื่อให้บอกว่าสร้างอะไร เช่น `hire` หรือ `defaultConfig`
2. เปลี่ยนเส้นทาง caller ไปยัง factory ทีละตัว แทนการเรียก constructor แต่ละครั้งด้วยการเรียก factory **รัน test** หลังแต่ละครั้ง
3. เมื่อ caller ทุกตัวผ่าน factory แล้ว คุณก็ได้จุดคอขวดจุดเดียว ตอนนี้จะเสริมอะไรก็ได้ เช่น validate argument คืน instance ที่ cache ไว้ หรือแตก branch ไปยัง subtype
4. ถ้ากำลังจะเพิ่ม subtype ก็สร้างขึ้นมา ย้าย behavior ของแต่ละ branch ไปไว้บน subtype ที่ตรงกัน แล้วให้ factory คืนตัวที่ถูกต้องตาม input
5. **รัน test** หลังเปลี่ยนแต่ละครั้ง signature ของ factory คงที่ในสายตา caller ถึงแม้สิ่งที่สร้างข้างหลังจะเปลี่ยนไปเรื่อย ๆ
6. หากภาษาอนุญาต ให้ทำ constructor ดิบเป็น private หรือ non-public เพื่อให้ factory เป็นทางเข้าที่รองรับเพียงทางเดียว

## ใช้เมื่อไหร่ / ข้อแลกเปลี่ยน

หยิบ factory มาใช้เมื่อการสร้างต้องการชื่อที่ชัดกว่าที่ชื่อ class จะให้ได้ เมื่อ type จริงที่ต้องสร้างขึ้นกับ input เมื่อคุณอยากซ่อน instance หรือรวมไว้เป็น pool หรือเมื่อต้องการ validate ก่อน object จะเกิดขึ้นจริง factory จะกลายเป็นประตูบานเดียวที่มีชื่อดี ๆ ให้ object เดินเข้าระบบ

ข้อแลกเปลี่ยนคือชั้นของการอ้อม: caller ไม่เห็น `new` และ type ที่เป็นรูปธรรมโดยตรงอีกต่อไป ซึ่งอาจทำให้การไล่ตาม code ชัดเจนน้อยลงเล็กน้อย สำหรับ type ค่าธรรมดาที่มีรูปทรงเดียวและไม่มีตัวแปร constructor โดยตรงเรียบง่ายกว่า และ factory ก็เป็นพิธีรีตองที่ไม่จำเป็น เพิ่ม factory เมื่อความแข็งทื่อของ constructor เริ่มทำให้คุณต้องเสีย — ไม่ใช่ก่อนหน้านั้น

## เนื้อหาที่เกี่ยวข้อง

- [Remove Flag Argument](/refactoring/th/simplifying-apis/remove-flag-argument/)
- [Introduce Parameter Object](/refactoring/th/simplifying-apis/introduce-parameter-object/)
- [Rename Function/Variable](/refactoring/th/simplifying-apis/rename-function/)

<Callout title="factory คือ constructor ที่ได้เลือกเอง">
constructor ต้องคืน type ของตัวเอง ภายใต้ชื่อของตัวเอง ส่วน factory function หลุดจากกฎทั้งสองข้อ คือตั้งชื่อว่า `hire` ก็ได้ คืน `Manager` หรือ `Engineer` ก็ได้ และถ้า input ผิดก็ปฏิเสธไม่สร้างเลยก็ยังได้ อิสระตรงนี้แหละคือประเด็นทั้งหมด
</Callout>

export const quizFactory = [
  {
    q: 'อะไรคือข้อจำกัดสำคัญของ constructor ที่ function factory หลีกเลี่ยงได้?',
    options: [
      'constructor รับ argument ไม่ได้',
      'constructor ต้องใช้ชื่อเดียวกับ type และคืน instance ของ type นั้นเป๊ะ ๆ เสมอ',
      'constructor test ไม่ได้',
      'constructor รันช้ากว่า function',
    ],
    answer: 1,
  },
  {
    q: 'อะไรคือขั้นตอนแรกที่ปลอดภัยเมื่อนำ factory เข้ามา?',
    options: [
      'ลบ constructor ทิ้งทันที',
      'เขียน factory ที่แค่เรียก constructor ที่มีอยู่ แล้วเปลี่ยนเส้นทาง caller ทีละตัว',
      'เปลี่ยนชื่อ subtype ทุกตัวก่อน',
      'ลบ test ทั้งหมดออก',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมการให้การสร้างทั้งหมดผ่าน factory เดียวจึงช่วยได้?',
    options: [
      'ทำให้พิมพ์ code สั้นลง',
      'สร้างจุดคอขวดจุดเดียวที่คุณจะ validate, cache หรือเลือก subtype ได้',
      'ทำให้ไม่ต้องมี class อีกต่อไป',
      'การันตีว่าสร้าง object ได้เร็วขึ้น',
    ],
    answer: 1,
  },
  {
    q: 'เมื่อใดที่ constructor ธรรมดาดีกว่า factory?',
    options: [
      'เมื่อ type มีตัวแปรหลายตัวที่เลือกตอน runtime',
      'สำหรับ type ค่าธรรมดาที่มีรูปทรงเดียวและไม่มีตัวแปร',
      'เมื่อคุณต้องตรวจสอบความถูกต้องของ input ก่อนสร้าง',
      'เมื่อคุณต้องการชื่อการสร้างที่สื่อความหมาย',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="simplifying-apis/replace-constructor-with-factory" questions={quizFactory} />

<ProgressTracker client:visible id="simplifying-apis/replace-constructor-with-factory" />
