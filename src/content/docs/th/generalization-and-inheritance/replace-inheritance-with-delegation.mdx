---
title: Replace Inheritance with Delegation
description: เมื่อ subclass ใช้ superclass แค่บางส่วน หรือความสัมพันธ์ "is-a" เป็นเรื่องโกหก ให้ถือ superclass เดิมไว้เป็น field แล้ว delegate เข้าไปแทน
sidebar:
  order: 4
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

export const delegationDiagram = `classDiagram
  class List {
    +add(item)
    +size() number
  }
  class Stack
  Stack --> List : holds & delegates
  note for Stack "Stack HAS-A List, not IS-A List"`;

## จุดประสงค์

**Replace Inheritance with Delegation** ปลด subclass ออกจาก superclass แล้วให้ถือ *field* ที่เก็บ instance ของ superclass เดิมไว้แทน จากนั้น class จะ delegate เฉพาะการเรียกที่ต้องใช้จริง ส่วนที่เหลือก็ไม่ต้องสนใจ ใช้ท่านี้เมื่อ subclass ใช้ interface ของ superclass แค่เสี้ยวเดียว หรือเมื่อ inheritance ที่เขียนไว้จริง ๆ แล้วคือความสัมพันธ์ "has-a" ที่แต่งตัวมาเป็น "is-a" ชื่อนี้เป็นชื่อในฉบับพิมพ์ครั้งแรก ส่วนฉบับพิมพ์ครั้งที่ 2 (2018) ของ Fowler เรียกท่าเดียวกันนี้ว่า *Replace Superclass with Delegate* ซึ่งบท [Replace Superclass with Delegation](/refactoring/th/generalization-and-inheritance/replace-superclass-with-delegation/) เดินผ่านด้วย `List` ที่ครบกว่า

## Code Smell

อาการคลาสสิกคือ **Refused Bequest** เช่น `Stack extends List` ทั้งที่ stack ไม่ควรเปิดให้ caller `insertAt` ตรงกลางหรือ `remove` จากก้นได้ การ inherit ทำให้ `Stack` เปิดเผย interface ทั้งชุดของ `List` แล้วทำลาย invariant ของตัวเองอย่างเงียบ ๆ ความสัมพันธ์ที่วางไว้จึงผิด เพราะ stack *มี* list ไม่ได้ *เป็น* list พอเปลี่ยนมาใช้ delegation แทน `Stack` ก็เปิดเผยแค่ `push` กับ `pop` และเก็บ list ไว้เป็น private

## ก่อน → หลัง

`Stack` ที่สร้างด้วยการ extend `List` จะปล่อยให้ operation ของ list รั่วออกไปหมด หลัง refactor จะเปลี่ยนเป็น *ถือ* list ไว้ แล้วเปิดเผยเฉพาะ operation ของ stack

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```ts
    // Before — Stack IS-A List, leaking add/get/removeAt to callers
    class List<T> {
      private items: T[] = [];
      add(item: T): void { this.items.push(item); }
      removeLast(): T | undefined { return this.items.pop(); }
      size(): number { return this.items.length; }
    }

    class Stack<T> extends List<T> {
      push(item: T): void { this.add(item); }
      pop(): T | undefined { return this.removeLast(); }
    }

    // After — Stack HAS-A List and delegates only what it needs
    class Stack<T> {
      private list = new List<T>();
      push(item: T): void { this.list.add(item); }
      pop(): T | undefined { return this.list.removeLast(); }
      size(): number { return this.list.size(); }
    }
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    # Before — Stack IS-A list, leaking insert/remove/index to callers
    class Stack(list):
        def push(self, item):
            self.append(item)

        def pop_top(self):
            return self.pop()

    # After — Stack HAS-A list and delegates only what it needs
    class Stack:
        def __init__(self):
            self._items = []

        def push(self, item):
            self._items.append(item)

        def pop_top(self):
            return self._items.pop()

        def size(self):
            return len(self._items)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go
    // Go favours composition by default. The "before" abuses embedding so a
    // Stack promotes every List method. The "after" holds the list as an
    // unexported field and exposes only Push/Pop — the idiomatic Go style.

    // Before — embedding promotes Add/RemoveLast/Size onto Stack
    type List struct{ items []int }

    func (l *List) Add(item int)    { l.items = append(l.items, item) }
    func (l *List) RemoveLast() int { n := len(l.items) - 1; v := l.items[n]; l.items = l.items[:n]; return v }
    func (l *List) Size() int       { return len(l.items) }

    type Stack struct{ List } // leaks Add, RemoveLast to callers

    // After — Stack holds a List and delegates only what it needs
    type Stack struct{ list List }

    func (s *Stack) Push(item int) { s.list.Add(item) }
    func (s *Stack) Pop() int      { return s.list.RemoveLast() }
    func (s *Stack) Size() int     { return s.list.Size() }
    ```
  </TabItem>
  <TabItem label="Rust">
    ```rust
    // Rust has no inheritance at all, so delegation is the only option — and
    // the idiomatic one. The Stack owns a Vec (the "list") privately and
    // exposes only push and pop.

    // Before (the closest mistake) — exposing the inner Vec publicly,
    // so callers can call insert/remove/truncate and break the invariant.
    pub struct Stack {
        pub items: Vec<i32>,
    }

    // After — the Vec is private; only stack operations are delegated outward.
    pub struct Stack {
        items: Vec<i32>,
    }

    impl Stack {
        pub fn new() -> Self {
            Stack { items: Vec::new() }
        }
        pub fn push(&mut self, item: i32) {
            self.items.push(item);
        }
        pub fn pop(&mut self) -> Option<i32> {
            self.items.pop()
        }
        pub fn size(&self) -> usize {
            self.items.len()
        }
    }
    ```
  </TabItem>
</Tabs>

<Mermaid code={delegationDiagram} title="Stack เลิกเป็น List แล้วเริ่มถือ List ไว้แทน" />

## กลไกการทำงาน

1. สร้าง field ใน subclass สำหรับถือ instance ของ superclass เดิม แล้วกำหนดค่าเริ่มต้นให้ จะสร้าง instance ใหม่ หรือใช้ตัว object เองถ้ากำลังแกะ self-reference อยู่ก็ได้
2. สำหรับแต่ละ method ของ superclass ที่ subclass ใช้จริง ให้เพิ่ม method ที่ delegate ซึ่งส่งต่อการเรียกไปยัง field นั้น
3. ลบความสัมพันธ์ `extends` / embedding ออก เพื่อให้ class ไม่ inherit interface ทั้งหมดของพ่อแม่อีกต่อไป
4. ปรับ caller ที่พึ่งพาสมาชิกที่ inherit มาซึ่งไม่ควรเข้าถึงได้ตั้งแต่แรก — นี่คือรอยรั่วที่คุณกำลังอุด
5. **รัน test** หลังเดินสาย method ที่ delegate แต่ละตัวเสร็จ
6. เมื่อ delegation อยู่ที่แล้ว คุณก็มีอิสระที่จะแคบ interface ที่เปิดเผยและปกป้อง invariant ที่ inheritance เดิมละเมิด

## ใช้เมื่อไหร่ / ข้อแลกเปลี่ยน

ใช้ท่านี้เมื่อ subclass ปฏิเสธมรดกที่ได้รับเสียเป็นส่วนใหญ่ เมื่อ inheritance ปล่อย operation ที่ทำลายกฎของ subclass เองรั่วออกมา หรือเมื่อ "is-a" ไม่เคยเป็นจริงตั้งแต่แรก หลังย้ายเสร็จ คุณจะคุมได้เป๊ะ ๆ ว่า operation ไหนเป็น public จึงบังคับ invariant ได้จริง

ต้นทุนคือ boilerplate ของการส่งต่อเล็กน้อย — หนึ่ง method สั้น ๆ ต่อหนึ่ง operation ที่ delegate นั่นเป็นราคาเล็ก ๆ และซื่อตรงสำหรับความสัมพันธ์ที่ถูกต้องแม่นยำ ใน Go นี่เป็นเพียงการเลือก field ที่ถือไว้แทน embedding ส่วนใน Rust delegation คือเส้นทาง *เดียว* ดังนั้น refactoring นี้จึงเป็นแค่ "เขียน Rust แบบ idiomatic ตั้งแต่แรก"

## เนื้อหาที่เกี่ยวข้อง

- [Replace Subclass with Delegation](/refactoring/th/generalization-and-inheritance/replace-subclass-with-delegation/)
- [Pull Up & Push Down](/refactoring/th/generalization-and-inheritance/pull-up-and-push-down/)
- [Extract Superclass & Interface](/refactoring/th/generalization-and-inheritance/extract-superclass-and-interface/)

<Callout title="เลือก composition มากกว่า inheritance">
inheritance มัดคุณไว้กับ interface *ทั้งชุด* ของ superclass รวมถึงทุกการเปลี่ยนแปลงในอนาคต ส่วน composition ให้คุณส่งต่อเฉพาะสิ่งที่ตั้งใจจะรองรับ ตามคำแนะนำใน *Design Patterns* (Gamma และคณะ, 1994) ที่ว่า "favor object composition over class inheritance" ถ้าไม่แน่ใจ ให้ถือ collaborator ไว้เป็น field แล้ว delegate เอา Go กับ Rust อบคำแนะนำนี้ไว้ในตัวภาษาแล้ว เพราะไม่มี inheritance ให้ใช้ผิด คุณจึงหยิบ delegation มาใช้โดยปริยาย และความสัมพันธ์ก็ซื่อตรงเสมอ
</Callout>

export const quizReplaceInh = [
  {
    q: 'Replace Inheritance with Delegation แก้ปัญหาใด?',
    options: [
      'function ที่ยาวเกินไป',
      'subclass ที่ใช้พ่อแม่เพียงบางส่วน หรือจำลองความสัมพันธ์ "is-a" ที่ผิด',
      'ดัชนีฐานข้อมูลที่หายไป',
      'สอง method ที่มีชื่อเดียวกัน',
    ],
    answer: 1,
  },
  {
    q: 'หลัง refactoring class นำพ่อแม่เดิมมาใช้ซ้ำอย่างไร?',
    options: [
      'คัดลอก code ของ superclass ทั้งหมดมา inline ไว้',
      'ถือ superclass เดิมไว้เป็น field แล้ว delegate เฉพาะการเรียกที่ต้องใช้',
      'ลบ superclass ทิ้งทั้งหมด',
      'เปลี่ยน superclass ให้เป็น abstract',
    ],
    answer: 1,
  },
  {
    q: 'ทำไม refactoring นี้จึง "ฟรี" ใน Rust?',
    options: [
      'Rust ปรับ inheritance ออกให้อัตโนมัติ',
      'Rust ไม่มี inheritance ดังนั้น delegation จึงเป็นวิธีเดียวและเป็นแบบ idiomatic อยู่แล้ว',
      'Rust ห้ามใช้ struct',
      'Rust กำหนดให้ทุก type ต้อง extend base trait',
    ],
    answer: 1,
  },
  {
    q: 'อะไรคือต้นทุนหลักของการเลือก delegation แทน inheritance?',
    options: [
      'ทำงานช้ากว่าเสมอ',
      'boilerplate ของการส่งต่อเล็กน้อย — หนึ่ง method สั้น ๆ ต่อหนึ่ง operation ที่ delegate',
      'คุณเสียความสามารถในการเขียน test',
      'class ไม่สามารถถือ field ได้อีกต่อไป',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="generalization-and-inheritance/replace-inheritance-with-delegation" questions={quizReplaceInh} />

<ProgressTracker client:visible id="generalization-and-inheritance/replace-inheritance-with-delegation" />
