---
title: Prototype
description: 'สร้างออบเจ็กต์ใหม่ด้วยการ clone ของที่มีอยู่ เลือก shallow หรือ deep copy อย่างตั้งใจ'
sidebar:
  order: 6
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## เจตนา

Prototype สร้างออบเจ็กต์ใหม่ด้วยการคัดลอก instance ที่มีอยู่และตั้งค่าไว้ครบถ้วนแล้ว ซึ่งก็คือ prototype แทนที่จะสร้างขึ้นมาใหม่ตั้งแต่ต้น

## ปัญหา

บางครั้งการสร้างออบเจ็กต์จาก constructor ของมันก็แพงหรือยุ่งยาก มันอาจต้องการการคำนวณที่แพง การตั้งค่าจำนวนมาก หรือความรู้ที่ผู้เรียกไม่มี ถ้าคุณถือ instance ที่เกือบจะเป็นอย่างที่คุณต้องการอยู่แล้ว การคัดลอกมันแล้วปรับแต่งสำเนาก็ง่ายกว่าการสร้างใหม่ Prototype ทำให้ "ขอตัวหนึ่งที่เหมือนตัวนี้" กลายเป็น first-class operation: ตัวออบเจ็กต์รู้วิธี clone ตัวเอง

การตัดสินใจที่นิยาม Prototype คือ **shallow เทียบกับ deep** shallow copy ทำสำเนาออบเจ็กต์ระดับบนสุด แต่แชร์ reference ที่ซ้อนอยู่ภายใน ดังนั้นการแก้ field ที่ซ้อนอยู่ในสำเนาก็จะเปลี่ยนต้นฉบับด้วย ส่วน deep copy ทำสำเนาทั้งกราฟ ทำให้สำเนาเป็นอิสระโดยสมบูรณ์ การเลือกผิดเป็นต้นเหตุคลาสสิกของบั๊ก "สำเนา" ที่แอบแชร์ list กับต้นฉบับจะสร้างความประหลาดใจให้คุณในครั้งแรกที่ฝ่ายใดฝ่ายหนึ่งถูกแก้ Prototype บังคับให้คุณเลือกสิ่งนั้นอย่างตั้งใจ

## โครงสร้าง

export const prototypeUml = `classDiagram
  class Prototype {
    <<interface>>
    +clone() Prototype
  }
  class Document {
    +title: string
    +tags: List
    +clone() Document
  }
  class Client {
    +duplicate(p) Prototype
  }
  Prototype <|.. Document
  Client ..> Prototype : clones`;

<Mermaid code={prototypeUml} title="prototype เปิดเผยการดำเนินการ clone ที่ client เรียกใช้" />

- **Prototype** — interface ที่ประกาศการดำเนินการ `clone`
- **Concrete Prototype** (`Document`) — implement `clone` เพื่อผลิตสำเนาของตัวเอง โดยตัดสินใจว่าจะ shallow หรือ deep
- **Client** — ได้ออบเจ็กต์ใหม่ด้วยการ clone prototype แทนที่จะเรียก constructor

## ตัวอย่าง

`Document` ที่มี list ของ tags เรา clone มันแบบ deep เพื่อให้การแก้ไข tags ของสำเนาไม่กระทบต้นฉบับ

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```ts
    class Document {
      constructor(
        public title: string,
        public tags: string[],
      ) {}

      // Deep clone: structuredClone copies the nested array too.
      clone(): Document {
        const copy = structuredClone({ title: this.title, tags: this.tags });
        return new Document(copy.title, copy.tags);
      }
    }

    const original = new Document('Report', ['draft', 'q2']);
    const copy = original.clone();
    copy.title = 'Report (revised)';
    copy.tags.push('final');

    console.log(original.title, original.tags); // Report ['draft', 'q2']
    console.log(copy.title, copy.tags); // Report (revised) ['draft', 'q2', 'final']
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import copy
    from dataclasses import dataclass, field


    @dataclass
    class Document:
        title: str
        tags: list[str] = field(default_factory=list)

        def clone(self) -> "Document":
            # deepcopy duplicates the nested list, so copies are independent.
            return copy.deepcopy(self)


    original = Document("Report", ["draft", "q2"])
    duplicate = original.clone()
    duplicate.title = "Report (revised)"
    duplicate.tags.append("final")

    print(original.title, original.tags)    # Report ['draft', 'q2']
    print(duplicate.title, duplicate.tags)  # Report (revised) ['draft', 'q2', 'final']
    ```
  </TabItem>
  <TabItem label="Go">
    ```go
    package main

    import "fmt"

    type Document struct {
        Title string
        Tags  []string
    }

    // Clone returns a deep copy: the tags slice is duplicated, not shared.
    func (d Document) Clone() Document {
        tags := make([]string, len(d.Tags))
        copy(tags, d.Tags)
        return Document{Title: d.Title, Tags: tags}
    }

    func main() {
        original := Document{Title: "Report", Tags: []string{"draft", "q2"}}
        duplicate := original.Clone()
        duplicate.Title = "Report (revised)"
        duplicate.Tags = append(duplicate.Tags, "final")

        fmt.Println(original.Title, original.Tags)   // Report [draft q2]
        fmt.Println(duplicate.Title, duplicate.Tags) // Report (revised) [draft q2 final]
    }
    ```
  </TabItem>
  <TabItem label="Rust">
    ```rust
    // Deriving Clone gives a deep copy: the Vec<String> is duplicated.
    #[derive(Clone)]
    struct Document {
        title: String,
        tags: Vec<String>,
    }

    fn main() {
        let original = Document {
            title: "Report".to_string(),
            tags: vec!["draft".to_string(), "q2".to_string()],
        };

        let mut duplicate = original.clone();
        duplicate.title = "Report (revised)".to_string();
        duplicate.tags.push("final".to_string());

        println!("{} {:?}", original.title, original.tags);   // Report ["draft", "q2"]
        println!("{} {:?}", duplicate.title, duplicate.tags); // Report (revised) ["draft", "q2", "final"]
    }
    ```
  </TabItem>
</Tabs>

## เมื่อไรควรใช้ / ข้อแลกเปลี่ยน

- **ข้อดี:** ถูกกว่าการสร้างใหม่เมื่อออบเจ็กต์สร้างได้แพงหรือซับซ้อน
- **ข้อดี:** client คัดลอกออบเจ็กต์ที่มีอยู่โดยไม่ต้องพึ่งพา concrete class หรืออาร์กิวเมนต์ของ constructor ของมัน
- **ข้อดี:** ให้คุณเก็บคลัง prototype ที่ตั้งค่าไว้ล่วงหน้าและปั๊มสร้างรูปแบบต่าง ๆ ออกมา
- **ข้อเสีย:** การ clone กราฟออบเจ็กต์ที่มี cycle หรือทรัพยากรที่แชร์กัน (ไฟล์ที่เปิดอยู่, socket) เป็นเรื่องยุ่งยาก
- **ข้อเสีย:** ความแตกต่างระหว่าง shallow กับ deep ผิดพลาดได้ง่าย ทำให้เกิดสำเนาที่แอบแชร์ state กัน

## Pattern ที่เกี่ยวข้อง

- [Builder](/design-patterns-from-zero-to-hero/th/creational/builder/) สร้างออบเจ็กต์ทีละขั้นตอน ในขณะที่ Prototype เริ่มจากตัวที่ทำสำเร็จไว้แล้ว
- [Abstract Factory](/design-patterns-from-zero-to-hero/th/creational/abstract-factory/) สามารถเก็บและคืน prototype แทนที่จะสร้าง product แต่ละตัวขึ้นใหม่

<Callout title="Shallow กับ deep — ตัดสินใจอย่างตั้งใจ">
ประเด็นทั้งหมดของ Prototype คือการควบคุมว่าอะไรถูกทำสำเนา shallow copy แชร์ reference ที่ซ้อนอยู่กับต้นฉบับ ส่วน deep copy เป็นอิสระโดยสมบูรณ์ ตัวอย่างทั้งหมดข้างต้นล้วน clone แบบ deep เพื่อให้การแก้สำเนาไม่เคยกระทบต้นฉบับ ยืนยันให้แน่ใจว่าคุณต้องการแบบไหนกันแน่ก่อนส่งมอบ
</Callout>

export const quiz = [
  {
    q: 'Prototype สร้างออบเจ็กต์ใหม่อย่างไร?',
    options: [
      'ด้วยการเรียก constructor ใหม่',
      'ด้วยการ clone instance ที่มีอยู่และตั้งค่าไว้แล้ว',
      'ด้วยการค้นหามันใน Singleton',
      'ด้วยการประกอบมันทีละขั้นตอน',
    ],
    answer: 1,
  },
  {
    q: 'ความแตกต่างระหว่าง shallow copy กับ deep copy คืออะไร?',
    options: [
      'shallow copy อ่านได้เร็วกว่าเสมอ',
      'shallow copy แชร์ reference ที่ซ้อนอยู่ ส่วน deep copy ทำสำเนาทั้งกราฟ',
      'deep copy แชร์ reference ที่ซ้อนอยู่ ส่วน shallow copy ทำสำเนาทุกอย่าง',
      'ไม่มีความแตกต่างสำหรับข้อมูลที่ immutable เท่านั้น',
    ],
    answer: 1,
  },
  {
    q: 'ทำไม shallow copy จึงก่อให้เกิดบั๊กได้?',
    options: [
      'มันใช้หน่วยความจำมากกว่า deep copy',
      'การแก้ field ที่ซ้อนอยู่ในสำเนาก็เปลี่ยนต้นฉบับด้วย',
      'มันไม่สามารถกำหนดค่าให้ตัวแปรได้',
      'มันบังคับให้ออบเจ็กต์กลายเป็น Singleton',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="creational/prototype" questions={quiz} />

<ProgressTracker client:visible id="creational/prototype" />
