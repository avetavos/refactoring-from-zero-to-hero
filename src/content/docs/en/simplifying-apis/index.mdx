---
title: Simplifying APIs
description: Reshape the way functions are called — clearer names, fewer arguments, no confusing flags — so an interface is a pleasure to use rather than a puzzle to decode.
sidebar:
  order: 1
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

export const overviewDiagram = `flowchart TD
  A["A function that is awkward to call"] --> B{"What makes it awkward?"}
  B -->|"The name misleads or hides intent"| C["Rename Function/Variable"]
  B -->|"Two near-clones differ by a value"| D["Parameterize Function"]
  B -->|"Too many arguments travel together"| E["Introduce Parameter Object"]
  B -->|"A boolean flips behaviour"| F["Remove Flag Argument"]
  B -->|"The constructor is too rigid"| G["Replace Constructor with Factory"]
  C --> H["An interface that explains itself"]
  D --> H
  E --> H
  F --> H
  G --> H`;

## Intent

The **simplifying APIs** family is about the *outside* of a function: the part every caller has to read and reason about. A function can have a flawless body and still be painful to use because its name lies, its argument list is a mile long, or a stray boolean silently changes what it does. These refactorings tidy the call site so the interface advertises exactly what it offers.

## The smell

You can usually feel a bad interface at the call site. You squint at an argument list and cannot remember which `true` means what. You find two functions that are ninety percent identical and only differ by a hard-coded number. You keep passing the same three values together, in the same order, to every function in a module. None of these are bugs — the code works — but each one taxes everyone who reads the call.

<Mermaid code={overviewDiagram} title="Choosing a simplifying-APIs move by what makes the call awkward" />

## What this module covers

Nine refactorings that make function and method interfaces clearer and easier to call:

- **[Rename Function/Variable](/refactoring-from-zero-to-hero/en/simplifying-apis/rename-function/)** — when a name misleads or under-explains, rename it and update every caller. Names are the cheapest documentation you will ever write.
- **[Parameterize Function](/refactoring-from-zero-to-hero/en/simplifying-apis/parameterize-function/)** — when two near-identical functions differ only by a literal value, merge them into one function that takes that value as a parameter.
- **[Introduce Parameter Object](/refactoring-from-zero-to-hero/en/simplifying-apis/introduce-parameter-object/)** — when a clump of arguments always travels together, group them into a single object so the relationship becomes explicit.
- **[Remove Flag Argument](/refactoring-from-zero-to-hero/en/simplifying-apis/remove-flag-argument/)** — when a boolean argument switches behaviour, split the function into two clearly-named functions.
- **[Replace Constructor with Factory Function](/refactoring-from-zero-to-hero/en/simplifying-apis/replace-constructor-with-factory/)** — when a constructor is too limiting, wrap it in a factory function that can pick a subtype and carry a meaningful name.
- **[Separate Query from Modifier](/refactoring-from-zero-to-hero/en/simplifying-apis/separate-query-from-modifier/)** — when a function both returns a value and changes state, split it into a pure query and a separate command.
- **[Preserve Whole Object](/refactoring-from-zero-to-hero/en/simplifying-apis/preserve-whole-object/)** — when a caller unpacks several fields from an object just to pass them along, hand over the whole object instead.
- **[Replace Parameter with Query](/refactoring-from-zero-to-hero/en/simplifying-apis/replace-parameter-with-query/)** — when a parameter is derivable from data the function already has, drop it and compute it inside.
- **[Remove Setting Method](/refactoring-from-zero-to-hero/en/simplifying-apis/remove-setting-method/)** — when a field should be fixed at construction, delete its setter to make the object immutable.

Each lesson shows the same before-and-after in TypeScript, Python, Go, and Rust, then walks through safe, ordered mechanics — running your tests between every step.

<Callout title="The interface is the contract">
The body of a function is private; its signature is a promise to everyone who calls it. Improving an interface pays off every single time the function is read or invoked — which, for a popular function, is far more often than its body is ever changed.
</Callout>

export const quizIndex = [
  {
    q: 'What part of a function do "simplifying APIs" refactorings primarily improve?',
    options: [
      'The internal implementation that callers never see',
      'The interface — the name, parameters, and how the function is called',
      'The build configuration',
      'The database schema behind the function',
    ],
    answer: 1,
  },
  {
    q: 'Which refactoring addresses two near-identical functions differing only by a literal value?',
    options: ['Remove Flag Argument', 'Parameterize Function', 'Rename Function', 'Introduce Parameter Object'],
    answer: 1,
  },
  {
    q: 'Why is improving an interface often worth more than improving a body?',
    options: [
      'The body is read more often than the interface',
      'A signature is read and invoked far more often than its body is changed',
      'Interfaces compile faster than bodies',
      'The compiler ignores function bodies',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="simplifying-apis/index" questions={quizIndex} />

<ProgressTracker client:visible id="simplifying-apis/index" />
